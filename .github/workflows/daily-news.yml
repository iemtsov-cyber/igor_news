name: News every 4 hours + GitHub Pages

on:
  workflow_dispatch:
  schedule:
    # Every 4 hours. UTC schedule becomes 03:00, 07:00, 11:00, 15:00, 19:00, 23:00 Moscow time.
    - cron: "0 */4 * * *"

permissions:
  contents: write
  pages: write
  id-token: write

concurrency:
  group: "pages"
  cancel-in-progress: false

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}

    steps:
      - name: Checkout repository
        uses: actions/checkout@v6
        with:
          fetch-depth: 0

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.13"
          cache: "pip"

      - name: Install dependencies
        run: pip install -r requirements.txt

      - name: Validate v8.1
        run: |
          python -m py_compile news.py
          python -m py_compile news_v8.py
          python -m py_compile news_v81.py

      - name: Build incremental edition from free RSS
        env:
          OPENROUTER_API_KEY: ${{ secrets.OPENROUTER_API_KEY }}
        run: python news_v81.py

      - name: Save feed, archive and memory
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add docs state.json
          if ! git diff --cached --quiet; then
            git commit -m "news: append four-hour RSS update"
            git pull --rebase origin main
            git push
          fi

      - name: Configure Pages
        uses: actions/configure-pages@v5

      - name: Upload Pages artifact
        uses: actions/upload-pages-artifact@v4
        with:
          path: docs

      - name: Deploy to GitHub Pages
        id: deployment
        uses: actions/deploy-pages@v4
